<#
    Orchestrates interactive flows for acquiring Windows ISO images.
    In the full implementation this script will offer manual and
    automated download modes.  At present it provides no behaviour.
#>
function Invoke-RiDIsoFlow {
    [CmdletBinding()] param()
    Write-Warning 'Invoke-RiDIsoFlow is not yet implemented.'
    return $null
}